*** 修改记录 ***
# 1、数据的切片序号DSID
切片序号从1开始
# 2、帧序号（seq）
帧序号不必须从0开始，只要是连续的就可以
seq不需要判断，seq同时与云端大模型和端侧大模型共用的，或者设备端因为看门狗重启，会重新计算seq
# 3、协议变为呼吸机与端侧AI串口通讯协议20260807

# 4、只有0x01和0x02的DSID不为0，其余命令都不用判断DSID
只有音频和文本需要关注DSID，其余的不用判断

8_11
# 5、type=01，state=01时，之前的消息作废，开始新一轮对话，未下发完的语音也不要再发了
# 6、每一帧下发的语音（type=0x11），间隔200毫秒

# 增加状态机来控制打断
```bash

TaskPhase
```